apiVersion: v1
kind: Service
metadata:
  name: violin-retail-service-prod
  namespace: devops
spec:
  selector:
    app: violin-retail
  # type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
